# Page Builder 开发入口

用户已授权开发首版，并要求把 Obsidian 中适用的开发与工程方法用于本项目、持续沉淀文档。主开发任务使用用户指定的 GPT-5.6 Sol / high。

## 开始与恢复任务

先读 [文档导航](docs/README.md)、[当前进度](docs/PROGRESS.md)，再按任务读取功能方案、工程流程、架构与验收表。先核对实际代码和运行版本，不把旧记录当作当前事实。常规实施选择由开发者推进，不重复询问已确认范围。

## 开发约束

- 正式开发目录是本项目。既有插件缓存和 Obsidian 笔记只读；保留用户文件。参考源码位置见 BASELINE_HANDOFF。
- 复用 ai-design-system 的真实 Renderer 与公开属性，访问该项目先读其 AGENTS.md。编辑器布局容器与组件内部结构分开。
- 页面结构是唯一持久化事实。手动编辑、AI 修改、撤销重做、保存和导出共用页面协议与校验，不建立互相矛盾的副本。
- 明确页面身份与版本；过期写入不能覆盖新内容；批量修改全部成功后才提交。失败不能留下半张页面或损坏已保存内容。
- 按可运行增量开发。页面操作、保存、导出等高影响逻辑必须有行为与失败路径验证；小型样式改动无需形式化单测。
- 发现缺陷先保留可复现证据，修复后补适当回归。不得弱化断言或隐藏错误来获得通过。
- 实现后单独评审接口两端、生命周期、异步与失败路径；自审如实标明。文献提到多 Agent 不构成启动子代理的指令。
- 浏览器可运行、安装包有效、Codex 已加载新版本、原生右侧入口可用分别验证。没有实际证据的项标为待验收。

## 文档维护

范围变更更新 FEATURE_PLAN；架构或重要取舍更新 ARCHITECTURE；验收结果更新 ACCEPTANCE 并链接证据；每个可运行增量、交接或阻塞更新 PROGRESS。通用方法维护在 DEVELOPMENT_WORKFLOW，不复制多份规则。

记录简短结论、理由和可复现证据，不记录冗长对话。过程改进先在本项目验证，重复有效后再考虑提炼成通用 Skill。文档本身不代表功能已实现。
