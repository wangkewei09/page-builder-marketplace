# 项目卡片工作台验收 / 2026-09-24

构建：0.1.1+codex.20260924080549。参考为用户提供的 Figma 首页截图；飞书链接无法直接读取，没有声称看过其登录后的实际页面。任务为既有设计系统内的项目管理功能增量，不修改组件库。

## 目标流程与环境

编辑器顶部项目入口 → 项目卡片 → 项目内页面 / 单独设置 → 保存后重开 / 返回编辑器。项目数据与用户数据隔离在临时目录；三个截图项目均为测试夹具。使用现有 Playwright + 本机 Chrome：会话未列 Browser skill，不建立新浏览器依赖。原生入口使用 MCP iframe 与严格 CSP（无 HTTP、只允许 data 图片/字体），独立 HTTP 流程另验。

## 结果

- 27 项 TypeScript 行为测试，MCP/package 2 项通过，构建/typecheck/插件与 Skill 校验通过。
- projects 浏览器专项：卡片搜索、无结果、收藏、上传封面、名称/说明保存；设置另一张卡片时不写入当前编辑项目；页面创建/复制/切换；延迟原位保存；隐藏悬浮组件操作栏；历史页面及跨库切换失败恢复。
- 磁盘行为：同 ID 多目录不串写；便携组件快照可空缓存恢复；坏资源拒绝；项目清单过期 revision 写入拒绝；并发设置仅一方成功且不改页面 JSON。
- 路径恢复：移动目录后仍保留卡片；错误项目路径拒绝且不改变原登记；相同项目验证后关联成功、失效旧快捷登记移除，文件内容未被移动/删除。
- browser 编辑器回归通过：添加、拖入、原位编辑、切变体、保存重开、撤销重做、预览。控制台 0 错误。
- native-resource 回归通过：严格 CSP、37 变体/4 类型、10 卡片几何对照、上下文与窄面板、启动错误可见；项目专项也断言无 HTTP 请求和无相关控制台错误。随后对工作台工具栏可见性和收藏按钮位置做了末尾修正，projects 专项再次通过。
- desktop 1440、narrow 680、mobile 420 截图与设置页已查看。网格按宽度换列；移动端导航允许横向滚动，内容无横向溢出。未把封面缩写误当成页面截图。源卡片采用裁切媒体布局，建议上传 16:9 封面。
- 官方 CLI 安装成功；16 个关键文件与构建逐字节一致，隔离的新安装版 MCP 已实际创建项目并保存设置 revision=1。当前真实 Codex 宿主仍是待重新加载状态，不据此声称用户旧面板已更新。
- 安装前后 353 个用户页面/历史/配置文件 SHA256 一致。备份路径和安装版本见 JSON 文件。

## 对照与自审

参考拓扑保留左导航、右侧搜索/操作和项目网格；颜色、图标、控件与卡片内部结构沿用现有组件库。未加入截图中的社区、团队或共享等不属于本次范围的功能。首页从顶部项目名称进入；未强制替换用户已经打开的编辑画布。

检查了 HTTP/MCP 参数两端、显式 workspaceId、清单锁/原子替换、旧清单兼容、错误恢复及 UI 挂载/释放。源 C-21 固定 240 字，首轮错误地设置 1000 导致挂载失败，已按源协议修正并让挂载失败显式中止表单。测试准备也修正了 ready 切换时的计数时序、歧义按钮定位及 macOS realpath 断言；没有删去失败断言或弱化错误检查。本次为自审，没有独立 Agent 评审。

## 范围边界

项目画布目前为页面入口列表，真正无限画布交互仍待实现。封面手动上传、路径手动关联，不后台扫描/自动截图、不移动项目文件；GitHub 仍使用已经克隆到本地的项目。源组件库没有改动，仍按原来的手动刷新机制更新。没有推送 GitHub。
