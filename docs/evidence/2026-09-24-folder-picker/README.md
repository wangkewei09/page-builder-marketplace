# 系统文件夹选择验证

构建 `0.1.1+codex.20260924085012`。本轮为自审。

- 31 个 TS 行为测试、2 个 MCP/package 测试通过；项目浏览器专项通过，零控制台错误。测试数据均在临时目录。
- `tests/directory-picker.test.ts` 检查目录规范化、中文/空格/特殊字符、初始位置不存在、取消、错误、单窗口、超时、进程信号与 HTTP 异源拒绝。
- `tests/projects.test.mjs` 只在系统窗口边界注入选择/取消/失败结果；其余使用真实 MCP 创建、保存、打开与重新关联。取消不改选择，未确认不创建，错误目录仍拒绝，现有项目流程通过。
- [截图](create.png) 来自隔离浏览器，已复核。使用真实 C-02 按钮/图标，没有路径输入框；它不是用户真实项目。
- 原生 JXA 适配器实际启动两次，均返回 pending 后在 180 秒超时关闭；CUA 不能绑定无应用包的 osascript，因此尚未证实系统窗口视觉和点击选择返回。已向用户请求反馈，未收到时保留待验收。
- 安装版检查与用户数据保护结果见同目录 JSON。当前已运行 Codex 后端是否重新加载，不能根据独立 MCP 通过来推断。

自审检查：界面按钮→HTTP/MCP→同一 DirectoryPicker；操作系统信号及晚到结果不会覆盖取消；确认仍经过 ProjectManager 同名/身份校验；不改变组件源和页面协议。关闭面板后的原生窗口最迟随 180 秒超时收束，当前无面板关闭即时通知协议。
